dependencies:
  pre:
    - ls ~/foo
  cache_directories:
    - "~/foo"
  override:
    - mkdir ~/foo
    - echo "asdf" > ~/foo/asdf
  post:
    - export FOO=bar

test:
  override:
    - echo $FOO
    - exit 0


