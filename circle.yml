dependencies:
  cache_directories:
    - "~/foo"
  pre:
    - ls ~/foo || echo "folder doesn't exist"
  cache_directories:
    - "~/foo"
  override:
    - mkdir ~/foo
    - echo "asdf" > ~/foo/asdf
  post:
    - export FOO=bar

test:
  override:
    - echo $FOO
    - exit 0


